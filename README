hosted at: https://felipefa6.github.io/prueba-tecnica/

running the proyect
---

# SET THE `production` VARIABLE TO FALSE!!!!

$ npm install
$ npm run dev


EXPLANATION ON WHY 'production' SHOULD CHANGE.
---

The 'public' folder contains all the requested
data being the excel file and the JSON file.
While hosting the page in github pages, the root
URL changes the path to '/prueba-tecnica'.

- localhost:3000/excel.xlsx
- github.io/prueba-tecnica/excel.xlsx

so, when trying to fetch the data from
'/excel.xlsx' the page returns a 404 error for not
found, so it shoul be '/prueba-tecnica/excel.xlsx'
